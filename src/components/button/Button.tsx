import React from 'react';
import styles from './button.module.scss'

export const Button = () => {
  return <button type="button" className={styles.button}>click</button>;
};

export default Button;
